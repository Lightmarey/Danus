interface:
  display_name: "Verify Proof"
  short_description: "Stage results and verify them via fact_submit_batch"
  default_prompt: "Use $verify-proof to durably stage related results and verify them via fact_submit_batch; each candidate is written iff independently accepted, and verifier feedback remains durable."

policy:
  allow_implicit_invocation: true
